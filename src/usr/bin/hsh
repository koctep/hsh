#!/bin/sh

if [ -n HSHROOT ] && [ -n HSHPACKETS ]; then
  p=`pwd`
  HSHPACKETS=`basename $p`
  HSHROOT=..
fi

HSHTPL=/usr/share/hsh/tpl HSHROOT=$HSHROOT HSHPACKETS=$HSHPACKETS \
  make -f /usr/share/hsh/Makefile HSHLOCALCFG=/etc/hsh "$@"
